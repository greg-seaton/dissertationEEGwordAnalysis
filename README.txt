most advanced content function NN is CNNcurrentBMP


datasets
- colour = 3 channels
- grayscale = 1 channels
- high gran = 1 channel, much more bins

- csv = just text
- bmp = image


environment.yml has everything but a lot of extra dependencies, need to clean it up!